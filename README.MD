> this is a electron + vue build

### run dev
```bash
yarn run start
```

### run build
```bash
# build package
yarn run build

# build ubuntu deb package
yarn run build:deb

# build code
yarn run build:web
```
